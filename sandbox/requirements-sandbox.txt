# Python deps installed inside the sandbox container.
# These are available to commands the agent runs via run_bash.
# Pinned for supply-chain stability — bump in a deliberate PR.

pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.6.9
mypy==1.11.2
httpx==0.27.2
pydantic==2.9.2
requests==2.32.3
